package com.prosecshane.quoteapp.domain.model

/**
 * Enum class that defines how often can the app remind the user to use itself.
 */
enum class NotificationPeriod {
    Off,
    Daily,
    Weekly,
    Monthly,
}
